<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AMGE Dashboard</title>
    <link 
        rel="stylesheet" 
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" 
        integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" 
        crossorigin="anonymous"
        referrerpolicy="no-referrer" 
    />
    <link rel="stylesheet" href="<%= base_url %>css/bulma.min.css">
    <link rel="stylesheet" href="<%= base_url %>css/bulma-dashboard.min.css">
    <script src="https://cdn.ckeditor.com/ckeditor5/32.0.0/classic/ckeditor.js"></script>    
</head>
<body>
    <div class="dashboard is-full-height">
        <%- include("templates/sidenav"); -%>
        <!-- main section -->
        <div class="dashboard-main is-scrollable">
            <%- include("templates/navbar"); -%>
            <section class="section">

                <p class="title is-size-2 is-spaced">
                    Noticias
                </p>
                <nav class="breadcrumb" aria-label="breadcrumbs">
                    <ul>
                        <li>
                            <a href="/dashboard">Inicio</a>
                        </li>
                        <li>
                            <a href="/dashboard/noticias">Noticias</a>
                        </li>
                        <li class="is-active">
                            <a href="#" aria-current="page">Editar noticia</a>
                        </li>
                    </ul>
                </nav>
                <form action="">
                    <div class="columns">
                        <div class="column">
                            <div class="field">
                                <label class="label">Título</label>
                                <div class="control">
                                    <input 
                                        class="input" 
                                        type="text" 
                                        name="titulo" 
                                        placeholder="Ingrese el título del post"
                                        id="titulo_input"
                                        <% if (typeof new_obj != "undefined") { %>
                                            value="<%= new_obj.new_title %>"
                                        <% } %>
                                        new_id="<%= new_obj.new_id %>"
                                    >
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="columns">
                        <div class="column">
                            <img src="<%= base_url %>news/thumbnails/<%= new_obj.new_thumbnail %>" alt="" id="miniatura_img">
                        </div>
                    </div>
                    <div class="columns">
                        <div class="column">
                            <label class="label">Miniatura: Proporción sugerida 4 X 3</label>
                            <div id="miniatura_input_div"  class="file has-name is-fullwidth">                                
                                <label class="file-label">
                                  <input class="file-input" type="file" name="miniatura_input" id="miniatura_input"/>
                                  <span class="file-cta">
                                    <span class="file-icon">
                                      <i class="fas fa-upload"></i>
                                    </span>
                                    <span class="file-label"> Selecciona un archivo… </span>
                                  </span>
                                  <span class="file-name"></span>
                                </label>
                              </div>
                        </div>
                    </div>
                    <div class="columns">
                        <div class="column">
                            <div class="field">
                                <label class="label">Contenido</label>
                                <div class="control">
                                    <textarea 
                                        class="Medium  textarea" 
                                        name="editor" 
                                        placeholder="Ingrese el contenido del post"
                                        id="editor" 
                                    ><%= new_obj.new_content %></textarea>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="columns">
                        <div class="column">
                            <div class="field">
                                <label class="label">Status</label>
                                <div class="control">
                                    <div class="select" >
                                        <select id="status_select">
                                            <option <%= new_obj.new_status ==  "A" ? "selected" : "" %> value="A">Activo</option>
                                            <option <%= new_obj.new_status ==  "I" ? "selected" : "" %> value="I">Inactivo</option>
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="columns">
                        <div class="column">
                            <div class="field is-grouped">
                                <div class="control">
                                    <button 
                                        class="button is-primary" 
                                        type="submit"
                                        id="guardar_btn"
                                    >
                                        Guardar Noticia
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
                
                
            </section>
            
            <%- include("templates/footer"); -%>            
        </div>
    </div>
    
    
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="<%= base_url %>js/dashboard/noticia_editar.js"></script>
    <script src="<%= base_url %>js/dashboard/dashboard.js"></script>
</body>
</html>